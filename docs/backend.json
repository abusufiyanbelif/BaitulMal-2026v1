{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the DocuExtract application who uploads and manages documents.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "creationDate": {
          "type": "string",
          "description": "The date and time when the user account was created.",
          "format": "date-time"
        },
        "lastModifiedDate": {
          "type": "string",
          "description": "The date and time when the user account was last modified.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "creationDate"
      ]
    },
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Represents an uploaded image or document file by a user for text and entity extraction.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Document entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who uploaded this document. (Relationship: User 1:N Document)"
        },
        "originalFileName": {
          "type": "string",
          "description": "The original file name of the uploaded document."
        },
        "uploadedAt": {
          "type": "string",
          "description": "The date and time when the document was uploaded.",
          "format": "date-time"
        },
        "documentType": {
          "type": "string",
          "description": "The classified type of the document (e.g., IDENTITY_PROOF, BILLING_STATEMENT, MEDICAL_REPORT, OTHER)."
        },
        "storagePath": {
          "type": "string",
          "description": "The URI or path where the original document file is stored.",
          "format": "uri"
        },
        "processingStatus": {
          "type": "string",
          "description": "The current processing status of the document (e.g., UPLOADED, PROCESSING, PROCESSED, FAILED)."
        },
        "lastModifiedAt": {
          "type": "string",
          "description": "The date and time when the document record was last modified.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "originalFileName",
        "uploadedAt",
        "documentType",
        "storagePath",
        "processingStatus"
      ]
    },
    "ExtractedText": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ExtractedText",
      "type": "object",
      "description": "Stores the raw text content extracted from a document using OCR and LLMs.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ExtractedText entity."
        },
        "documentId": {
          "type": "string",
          "description": "Reference to the Document from which this text was extracted. (Relationship: Document 1:1 ExtractedText)"
        },
        "content": {
          "type": "string",
          "description": "The full text content extracted from the document."
        },
        "extractedAt": {
          "type": "string",
          "description": "The date and time when the text was initially extracted.",
          "format": "date-time"
        },
        "lastEditedAt": {
          "type": "string",
          "description": "The date and time when the extracted text was last manually edited by a user.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "documentId",
        "content",
        "extractedAt"
      ]
    },
    "IdentityProofExtraction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "IdentityProofExtraction",
      "type": "object",
      "description": "Stores structured details extracted from identity proof documents (e.g., ID cards, passports).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the IdentityProofExtraction entity."
        },
        "documentId": {
          "type": "string",
          "description": "Reference to the Document from which these details were extracted. (Relationship: Document 1:1 IdentityProofExtraction)"
        },
        "fullName": {
          "type": "string",
          "description": "The full name of the person as extracted from the document."
        },
        "dateOfBirth": {
          "type": "string",
          "description": "The date of birth of the person.",
          "format": "date"
        },
        "address": {
          "type": "string",
          "description": "The residential address as extracted from the document."
        },
        "idNumber": {
          "type": "string",
          "description": "The identification number (e.g., passport number, driver's license number)."
        },
        "documentIssueDate": {
          "type": "string",
          "description": "The date when the identity document was issued.",
          "format": "date"
        },
        "documentExpiryDate": {
          "type": "string",
          "description": "The date when the identity document expires.",
          "format": "date"
        },
        "extractedAt": {
          "type": "string",
          "description": "The date and time when these details were extracted.",
          "format": "date-time"
        },
        "lastModifiedAt": {
          "type": "string",
          "description": "The date and time when these extracted details were last modified (e.g., via user correction).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "documentId",
        "fullName",
        "idNumber",
        "extractedAt"
      ]
    },
    "BillingStatementExtraction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BillingStatementExtraction",
      "type": "object",
      "description": "Stores structured details extracted from billing statements and invoices.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BillingStatementExtraction entity."
        },
        "documentId": {
          "type": "string",
          "description": "Reference to the Document from which these billing details were extracted. (Relationship: Document 1:1 BillingStatementExtraction)"
        },
        "vendorName": {
          "type": "string",
          "description": "The name of the vendor or service provider on the bill."
        },
        "invoiceNumber": {
          "type": "string",
          "description": "The unique invoice number of the billing statement."
        },
        "invoiceDate": {
          "type": "string",
          "description": "The date when the invoice was issued.",
          "format": "date"
        },
        "dueDate": {
          "type": "string",
          "description": "The date when the payment for the invoice is due.",
          "format": "date"
        },
        "totalAmount": {
          "type": "number",
          "description": "The total amount of the bill."
        },
        "currency": {
          "type": "string",
          "description": "The currency in which the total amount is denominated (e.g., USD, EUR)."
        },
        "extractedAt": {
          "type": "string",
          "description": "The date and time when these billing details were extracted.",
          "format": "date-time"
        },
        "lastModifiedAt": {
          "type": "string",
          "description": "The date and time when these extracted billing details were last modified (e.g., via user correction).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "documentId",
        "vendorName",
        "invoiceNumber",
        "invoiceDate",
        "totalAmount",
        "currency",
        "extractedAt"
      ]
    },
    "BillingLineItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BillingLineItem",
      "type": "object",
      "description": "Represents individual line items within a BillingStatementExtraction.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BillingLineItem entity."
        },
        "billingStatementExtractionId": {
          "type": "string",
          "description": "Reference to the BillingStatementExtraction this line item belongs to. (Relationship: BillingStatementExtraction 1:N BillingLineItem)"
        },
        "description": {
          "type": "string",
          "description": "A description of the product or service for this line item."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the item or service."
        },
        "unitPrice": {
          "type": "number",
          "description": "The price per unit of the item or service."
        },
        "totalPrice": {
          "type": "number",
          "description": "The total price for this line item (quantity * unitPrice)."
        },
        "extractedAt": {
          "type": "string",
          "description": "The date and time when this line item was extracted.",
          "format": "date-time"
        },
        "lastModifiedAt": {
          "type": "string",
          "description": "The date and time when this line item was last modified (e.g., via user correction).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "billingStatementExtractionId",
        "description",
        "quantity",
        "unitPrice",
        "totalPrice",
        "extractedAt"
      ]
    },
    "MedicalReportExtraction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MedicalReportExtraction",
      "type": "object",
      "description": "Stores structured diagnostic and patient details extracted from hospital or medical reports.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the MedicalReportExtraction entity."
        },
        "documentId": {
          "type": "string",
          "description": "Reference to the Document from which these medical details were extracted. (Relationship: Document 1:1 MedicalReportExtraction)"
        },
        "patientName": {
          "type": "string",
          "description": "The full name of the patient mentioned in the report."
        },
        "reportDate": {
          "type": "string",
          "description": "The date when the medical report was generated.",
          "format": "date"
        },
        "diagnoses": {
          "type": "array",
          "description": "A list of diagnoses identified in the medical report.",
          "items": {
            "type": "string"
          }
        },
        "symptoms": {
          "type": "array",
          "description": "A list of symptoms reported or observed in the medical report.",
          "items": {
            "type": "string"
          }
        },
        "prescriptions": {
          "type": "array",
          "description": "A list of prescribed medications or treatments.",
          "items": {
            "type": "string"
          }
        },
        "doctorName": {
          "type": "string",
          "description": "The name of the doctor who authored or signed the report."
        },
        "hospitalName": {
          "type": "string",
          "description": "The name of the hospital or clinic where the report was issued."
        },
        "extractedAt": {
          "type": "string",
          "description": "The date and time when these medical details were extracted.",
          "format": "date-time"
        },
        "lastModifiedAt": {
          "type": "string",
          "description": "The date and time when these extracted medical details were last modified (e.g., via user correction).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "documentId",
        "patientName",
        "reportDate",
        "extractedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Top-level collection storing user profiles. Access is restricted to the user matching the {userId} wildcard. This path uses the user's UID as the document ID."
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}",
        "definition": {
          "entityName": "Document",
          "schema": {
            "$ref": "#/backend/entities/Document"
          },
          "description": "Subcollection storing documents uploaded by a specific user. Authorization is path-based, meaning only the owner (`{userId}`) can access their documents.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns this document."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}/extractedTexts/{extractedTextId}",
        "definition": {
          "entityName": "ExtractedText",
          "schema": {
            "$ref": "#/backend/entities/ExtractedText"
          },
          "description": "Subcollection storing the raw extracted text for a specific document. Authorization is inherited from the parent document and user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the parent document."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the parent document."
            },
            {
              "name": "extractedTextId",
              "description": "The unique ID of the extracted text record."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}/identityProofExtractions/{identityProofExtractionId}",
        "definition": {
          "entityName": "IdentityProofExtraction",
          "schema": {
            "$ref": "#/backend/entities/IdentityProofExtraction"
          },
          "description": "Subcollection storing structured data extracted from identity proof documents. Authorization is inherited from the parent document and user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the parent document."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the parent document."
            },
            {
              "name": "identityProofExtractionId",
              "description": "The unique ID of the identity proof extraction record."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}/billingStatementExtractions/{billingStatementExtractionId}",
        "definition": {
          "entityName": "BillingStatementExtraction",
          "schema": {
            "$ref": "#/backend/entities/BillingStatementExtraction"
          },
          "description": "Subcollection storing structured data extracted from billing statements. Authorization is inherited from the parent document and user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the parent document."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the parent document."
            },
            {
              "name": "billingStatementExtractionId",
              "description": "The unique ID of the billing statement extraction record."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}/billingStatementExtractions/{billingStatementExtractionId}/lineItems/{billingLineItemId}",
        "definition": {
          "entityName": "BillingLineItem",
          "schema": {
            "$ref": "#/backend/entities/BillingLineItem"
          },
          "description": "Subcollection storing individual line items for a specific billing statement extraction. Authorization is inherited from the parent billing statement, document, and user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the parent document."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the parent document."
            },
            {
              "name": "billingStatementExtractionId",
              "description": "The unique ID of the parent billing statement extraction."
            },
            {
              "name": "billingLineItemId",
              "description": "The unique ID of the billing line item record."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/documents/{documentId}/medicalReportExtractions/{medicalReportExtractionId}",
        "definition": {
          "entityName": "MedicalReportExtraction",
          "schema": {
            "$ref": "#/backend/entities/MedicalReportExtraction"
          },
          "description": "Subcollection storing structured data extracted from medical reports. Authorization is inherited from the parent document and user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user who owns the parent document."
            },
            {
              "name": "documentId",
              "description": "The unique ID of the parent document."
            },
            {
              "name": "medicalReportExtractionId",
              "description": "The unique ID of the medical report extraction record."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure for DocuExtract is designed with a strong emphasis on Authorization Independence and Query-Ability for Security Rules (QAPs). All user-specific data is strictly segregated under a hierarchical path model, directly leveraging Firebase Authentication's `request.auth.uid` for robust, easily auditable security.\n\n1.  **Authorization Independence (CRITICAL):** This structure achieves authorization independence by deeply nesting all user-owned documents and their related extractions under the respective user's path: `/users/{userId}/documents/{documentId}/...`. The `userId` parameter in the path directly corresponds to `request.auth.uid`. This design eliminates the need for expensive and complex `get()` calls in security rules to check ownership or relationships. For example, to access a document, the rule simply verifies `match /users/{userId}/documents/{documentId} { allow read, write: if request.auth.uid == userId; }`. This makes authorization checks atomic, performant, and simple to debug, as all required authorization context (`userId`) is present in the request path.\n\n2.  **Query-Ability for Security Rules (QAPs):** By organizing data primarily under `/users/{userId}`, we inherently solve the QAPs problem. When a user queries for their documents (e.g., `db.collection('users').doc(request.auth.uid).collection('documents').get()`), the query path itself (`/users/{request.auth.uid}/documents`) acts as the security filter. The security rules can then grant access based on `request.auth.uid == userId`, ensuring that users can only ever list or access documents directly belonging to them. There are no 'global' collections that would require `where` clauses in rules (which Firestore rules cannot enforce as filters), thus preventing accidental data exposure.\n\n3.  **Structural Segregation:** Each collection (`users`, `documents`, `extractedTexts`, `identityProofExtractions`, `billingStatementExtractions`, `lineItems`, `medicalReportExtractions`) has a homogeneous security posture: all documents within these nested paths are owned by the `userId` in the path segment. This avoids mixing data with different access requirements and keeps rules concise and focused.\n\n4.  **Data Clarity and Predictability:** Wildcard names (`{userId}`, `{documentId}`) are descriptive. Entities like `Document` use an explicit `processingStatus` field for state management, adhering to predictable schema principles. Denormalization for authorization context is achieved purely through the hierarchical path, meaning no redundant `ownerId` fields are strictly necessary within the nested documents themselves, although including `userId` for indexing or easier client-side querying might be beneficial if not made part of the path, but in this case, the path itself provides it.\n\nGiven the user's specific question \"do we have donor collection?\", based on the provided entities and application requirements for DocuExtract, there is no entity or functionality related to 'donors'. Therefore, no 'donor' collection is included in this design."
  }
}