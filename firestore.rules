rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // UNIVERSAL ACCESS FOR AUTHENTICATED USERS (TESTING PHASE)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}