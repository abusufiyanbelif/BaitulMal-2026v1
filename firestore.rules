rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read access for institutional transparency
      allow read: if true;
      // Allow authenticated members full management access
      allow write: if request.auth != null;
    }
  }
}