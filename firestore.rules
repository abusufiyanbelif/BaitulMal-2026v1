rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all institutional data for transparency
    // Allow authenticated members full control over data management
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}