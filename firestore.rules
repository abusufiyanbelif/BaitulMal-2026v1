rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public transparency: Allow anyone to read verified institutional data
    match /{document=**} {
      allow read: if true;
      // Member empowerment: Allow any authenticated organizational member to manage content
      allow write, delete: if request.auth != null;
    }
  }
}