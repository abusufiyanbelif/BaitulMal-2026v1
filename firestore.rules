rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic institutional rules: Public transparency for verified data, Member control for management.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}