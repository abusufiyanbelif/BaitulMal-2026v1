rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Unrestricted access for prototype development as requested.
    // UI-level role checks in the application will govern visible features.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}