rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // UNRESTRICTED ACCESS FOR PROTOTYPE TESTING
      // Security governed by UI-level role checks
      allow read, write: if true;
    }
  }
}