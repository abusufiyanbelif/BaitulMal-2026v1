rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read access for institutional transparency (verified stats, public campaigns, recent donations)
      allow read: if true;
      
      // Require authentication for all write/delete operations
      allow write: if request.auth != null;
    }
  }
}