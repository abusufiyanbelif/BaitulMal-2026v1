rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // UNRESTRICTED ACCESS: As requested, allowing all operations for prototype testing.
      // Access control is managed at the UI/Application layer.
      allow read, write: if true;
    }
  }
}