release: v2026.09.12.3

Type: Feature, Bug Fix & UI Polish
Message: Comprehensive Case ID Notification & Public Donation Table Alignment. (1) Alert Notification Case ID Display: Explicitly embedded `🆔 Case ID: {caseId}` into all automated WhatsApp, Telegram, and In-App alert notifications (`generateDetailedPayload`, `notifyApprovalFinalizedAction`, `notifyVerificationUpdateAction`, `requestVerificationAction`). Displayed Case ID badges (`Case ID: {caseId || id}`) on task items in Notification Center (`NotificationBell`). (2) Unified Linked Donation Resolution: Implemented `isDonationLinkedToInitiative` and `getDonationLinkForInitiative` in `src/lib/utils.ts` to support multi-attribute matching across document IDs, Case IDs, prefixed keys (`campaign_*`, `lead_*`), and `linkSplit` entries. (3) Public Summary Donation Tables: Replaced single ID filter in Public Campaign (`campaign-public/[campaignId]/summary`) and Public Appeal (`leads-public/[leadId]/summary`) pages to ensure all linked donations display cleanly in public contribution history tables under `Case / Ref ID`.
Reference: MB-CASE-ID-NOTIFICATIONS-2026, MB-PUBLIC-DONATIONS-MATCH-2026
Branch: test_bmss_26
Commit: latest

Verification Steps:
1. Notification Verification: Verified alert notifications for verification requests, approvals, rejections, and task list items include Case IDs.
2. Public Summary Tables: Verified public summary donation tables display linked donations correctly when filtered by Case ID.
3. Type Safety & Build Verification: Executed `npx tsc --noEmit` (0 errors) and `npm run build` (standalone bundle generated cleanly with exit code 0).

---
*Build Summary Updated: 12/9/2026, 1:00:00 am*