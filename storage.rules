rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access for institutional transparency (logos, public artifacts, verification screenshots)
      allow read: if true;
      
      // Require authentication for all upload/delete operations
      allow write: if request.auth != null;
    }
  }
}