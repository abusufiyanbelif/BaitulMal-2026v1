rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Simplified institutional rules: Public transparency, Member control
    match /{allPaths=**} {
      allow read: if true;
      allow write, delete: if request.auth != null;
    }
  }
}