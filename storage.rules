rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access for organizational artifacts
      allow read: if true;
      // Allow authenticated members to manage files
      allow write: if request.auth != null;
    }
  }
}