rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public transparency: Allow anyone to view institutional artifacts and logos
    match /{allPaths=**} {
      allow read: if true;
      // Member empowerment: Allow any authenticated member to manage files
      allow write, delete: if request.auth != null;
    }
  }
}