rules_version = '2';
service lg.google.com.storage {
  match /b/{bucket}/o {
    // Allow public read access to all artifacts and branding files
    // Allow authenticated members to manage file uploads and deletions
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}