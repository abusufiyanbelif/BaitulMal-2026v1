rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow global public read access for artifacts and branding
    match /{allPaths=**} {
      allow read: if true;
      allow write, delete: if request.auth != null;
    }
  }
}