rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Institutional file policy: Public transparency for artifacts, Member control for uploads.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}